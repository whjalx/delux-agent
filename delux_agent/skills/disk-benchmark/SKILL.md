# Disk Benchmark

Summary: A professional-grade disk performance benchmark tool written in C. Measures sequential write and read speeds to evaluate disk performance "como un rayo".

## Usage
Run with a file size (in MB): `@disk-benchmark 100`

## Implementation
Written in C for direct system call access, providing accurate raw performance metrics without the overhead of higher-level languages.
